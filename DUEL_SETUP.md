# 🎮 Настройка дуэлей - Полная инструкция

## ✅ Что уже работает:
- ✅ Создание дуэлей через API
- ✅ Ручное принятие через Match ID
- ✅ История дуэлей
- ✅ Определение победителя

## 🚀 Настройка автоматического запуска через ссылку

### ШАГ 1: Создайте Web App в BotFather

1. Откройте @BotFather в Telegram
2. Отправьте команду:
   ```
   /newapp
   ```

3. Выберите **@monkey_test_crypto_bot**

4. **Короткое имя (латиницей, без пробелов):**
   ```
   game
   ```

5. **Название приложения:**
   ```
   Crypto Monkey
   ```

6. **Описание:**
   ```
   Jump as high as you can and challenge your friends to duels!
   ```

7. **Загрузите фото** (512x512 пикселей) - можно пропустить

8. **Web App URL** (ваш Vercel URL):
   ```
   https://ваш-домен.vercel.app
   ```
   
   Узнать ваш URL: https://vercel.com/dashboard → выберите проект → скопируйте домен

9. **Короткое имя для URL:**
   ```
   game
   ```

✅ Готово! Теперь у вас есть ссылка:
```
https://t.me/monkey_test_crypto_bot/game
```

---

### ШАГ 2: Настройте переменные окружения на Render

1. Зайдите на https://dashboard.render.com
2. Найдите сервис **monkey-flipper-api**
3. **Environment** → **Add Environment Variable**
4. Добавьте 2 переменные:

```
WEB_APP_NAME=game
WEB_APP_URL=https://ваш-домен.vercel.app
```

5. Сохраните → сервис перезапустится (2-3 минуты)

---

### ШАГ 3: Протестируйте

1. **Откройте игру** через бота
2. **Создайте вызов** на дуэль
3. **Проверьте ссылку** - должна быть такой:
   ```
   https://t.me/monkey_test_crypto_bot/game?startapp=duel_123456_abc
   ```

4. **Отправьте другу**
5. Друг нажимает → **автоматически открывается игра и принимается вызов!** ✅

---

## 🎯 Как работает:

### Вариант 1: Автоматический (через ссылку)
```
Игрок 1: Создаёт вызов
         ↓
Получает: https://t.me/bot/game?startapp=duel_123
         ↓
Отправляет другу через "Share in Telegram"
         ↓
Игрок 2: Нажимает на ссылку
         ↓
Telegram: Автоматически открывает Web App
         ↓
Игра: Читает параметр startapp → принимает вызов → запускается!
```

### Вариант 2: Ручной (fallback)
```
Игрок 1: Создаёт вызов
         ↓
Нажимает: "Copy Match ID"
         ↓
Отправляет ID другу (любой мессенджер)
         ↓
Игрок 2: Открывает игру → Duels → Accept Challenge
         ↓
Вставляет: duel_123456_abc
         ↓
Игра: Проверяет Match ID → принимает вызов → запускается!
```

---

## 🔧 Troubleshooting

### Проблема: "404 NOT FOUND" при открытии Web App
**Решение:** Проверьте URL в BotFather - должен совпадать с Vercel URL

### Проблема: Ссылка не содержит `/game?startapp=`
**Решение:** 
1. Убедитесь что выполнили `/newapp` в BotFather
2. Проверьте переменные окружения на Render
3. Перезапустите API сервис на Render

### Проблема: Параметр не передаётся
**Решение:** Используйте "Copy Match ID" - это всегда работает 100%

---

## 📝 Примечания

- Ссылки вида `t.me/bot/game?startapp=` работают только в **Telegram клиентах**
- В браузере используется fallback: прямая ссылка с `?matchId=`
- Оба способа полностью поддерживаются кодом
- Код не ломается - если что-то не работает, остаётся ручной ввод

---

## ✨ Готово!

Теперь у вас **2 способа** принять вызов:
1. ⚡ **Автоматический** - по ссылке (1 клик)
2. 📋 **Ручной** - через Match ID (копипаста)

Оба работают параллельно, не мешая друг другу!
