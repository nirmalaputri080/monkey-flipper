# 🎬 Быстрый старт: Вступительное видео

## 📝 Что было добавлено:

✅ Функции в `telegram-stars-real.js`:
- `showIntroVideo()` - показ MP4 видео
- `showIntroAnimation()` - показ GIF анимации

✅ API endpoints в `server-api.js`:
- `POST /api/send-intro-video` - отправка видео
- `GET /api/check-intro/:userId` - проверка просмотра

✅ Telegram Bot команды:
- `/start` - показывает intro при первом запуске
- `/video` - пересмотреть intro видео

✅ База данных:
- Таблица `users` с колонкой `intro_seen`

---

## 🚀 Как использовать:

### Шаг 1: Подготовьте видео

```bash
# Перейдите в папку проекта
cd /Users/artursitikov/Downloads/monkey_flipper_test_key_1/monkey-flipper-api

# Положите ваше видео в assets/
# Файл должен называться: intro.mp4 или intro.gif
```

**Требования к видео:**
- Формат: MP4 (H.264) или GIF
- Размер: до 20 МБ
- Длительность: 5-15 секунд
- Разрешение: 1080x1920 (вертикальное) или любое

**Нет видео? Создайте простое:**
```bash
# Конвертировать существующее видео
ffmpeg -i your-video.mov -c:v libx264 -crf 28 -vf "scale=1080:1920:force_original_aspect_ratio=decrease,pad=1080:1920:(ow-iw)/2:(oh-ih)/2" assets/intro.mp4

# Или создайте GIF
ffmpeg -i your-video.mov -vf "fps=10,scale=480:-1" assets/intro.gif
```

### Шаг 2: Настройте `.env`

Откройте `monkey-flipper-api/.env` и установите:

```env
# URL вашей игры (для кнопки "Начать игру")
GAME_URL=https://your-game.vercel.app

# Включить Telegram bot polling (для команд /start, /video)
ENABLE_BOT_POLLING=true

# Ваш Bot Token уже есть
BOT_TOKEN=8178845934:AAGXqF1EF73-J3C3Qnw4FKQ1cy74gZRuAz4
```

### Шаг 3: Запустите сервер

```bash
cd monkey-flipper-api
node server-api.js
```

Вы должны увидеть:
```
✅ Database tables created successfully
🤖 Telegram Bot запущен в режиме polling
📹 Вступительное видео: Включено
API server listening on 3001
📹 Intro Video API: /api/send-intro-video
```

### Шаг 4: Протестируйте

**Вариант A: Через Telegram бота**
1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Бот пришлет видео с кнопкой "🎮 Начать игру"
4. При повторном `/start` - сразу кнопка игры (видео показывается только раз)

**Вариант B: Через API**
```bash
# Отправить видео пользователю
curl -X POST http://localhost:3001/api/send-intro-video \
  -H "Content-Type: application/json" \
  -d '{"userId": "YOUR_TELEGRAM_ID", "videoType": "mp4"}'

# Проверить, видел ли пользователь intro
curl http://localhost:3001/api/check-intro/YOUR_TELEGRAM_ID
```

**Вариант C: Используйте тестовый скрипт**
```bash
# Отредактируйте USER_ID в скрипте
nano test-intro-video.sh

# Запустите
bash test-intro-video.sh
```

---

## 📱 Интеграция в клиент (index.html)

Добавьте проверку при загрузке игры:

```javascript
// В начале index.html, после Telegram.WebApp.ready()
const tg = window.Telegram.WebApp;
const userId = tg.initDataUnsafe?.user?.id;

if (userId) {
    // Проверяем, видел ли пользователь intro
    fetch(`https://your-api.com/api/check-intro/${userId}`)
        .then(res => res.json())
        .then(data => {
            if (!data.introSeen) {
                // Первый запуск - запросить показ видео
                fetch('https://your-api.com/api/send-intro-video', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ userId, videoType: 'mp4' })
                });
                
                // Показать лоадер или сообщение
                document.body.innerHTML = '<div style="padding:20px; text-align:center;">📹 Отправляем видео... Проверьте чат с ботом!</div>';
            } else {
                // Уже видел - запускаем игру
                startGame();
            }
        });
}
```

---

## 🎨 Где взять видео для intro?

### 1. Создайте сами:
- **Canva**: canva.com (шаблоны для видео)
- **Figma** + **Lottie**: анимации
- **Adobe Express**: быстрые видео

### 2. Используйте AI:
- **Runway ML**: текст → видео
- **Pika Labs**: AI видео генератор
- **Leonardo.ai**: анимации

### 3. Готовые ресурсы:
- **Mixkit**: бесплатные видео
- **Pexels Videos**: стоковые ролики
- **Giphy**: GIF анимации

### 4. Простой текст + музыка:
```bash
# Создать видео с текстом
ffmpeg -f lavfi -i color=c=black:s=1080x1920:d=5 \
  -vf "drawtext=text='Monkey Flipper':fontsize=80:fontcolor=white:x=(w-text_w)/2:y=(h-text_h)/2" \
  assets/intro.mp4
```

---

## 🔧 Настройка для production

### 1. Обновите переменные окружения:

```env
# Production URL вашей игры
GAME_URL=https://monkey-flipper.vercel.app

# Включите bot polling (или используйте webhook)
ENABLE_BOT_POLLING=true
```

### 2. Загрузите видео на CDN:

**Cloudflare R2 / AWS S3 / Vercel Blob:**
```javascript
// Вместо локального файла используйте URL
const VIDEO_URL = 'https://cdn.yoursite.com/intro.mp4';
await telegramStars.showIntroVideo(userId, VIDEO_URL, gameUrl);
```

### 3. Используйте file_id (рекомендуется):

После первой отправки получите `file_id`:
```javascript
const message = await bot.sendVideo(userId, './assets/intro.mp4');
console.log('File ID:', message.video.file_id);
// Сохраните этот ID в .env как INTRO_VIDEO_FILE_ID
```

Затем используйте:
```javascript
await telegramStars.showIntroVideo(userId, process.env.INTRO_VIDEO_FILE_ID, gameUrl);
```

**Преимущества file_id:**
- ⚡ Мгновенная отправка (Telegram уже имеет файл)
- 💾 Не нужно хранить видео на сервере
- 🆓 Бесплатный CDN от Telegram

---

## 📊 Проверка работы

### В логах сервера вы увидите:
```
🎮 Новый пользователь: username (123456789)
📹 Отправка intro video пользователю 123456789
✅ Вступительное видео отправлено пользователю 123456789
```

### В базе данных:
```sql
SELECT telegram_id, username, intro_seen, created_at 
FROM users 
WHERE intro_seen = true;
```

### В Telegram:
- Пользователь получит видео
- Под видео кнопка "🎮 Начать игру"
- При клике открывается ваша игра

---

## 🐛 Troubleshooting

**❌ "File too large"**
```bash
# Сожмите видео
ffmpeg -i intro.mp4 -c:v libx264 -crf 28 -preset fast assets/intro-compressed.mp4
```

**❌ "Bot not found"**
- Проверьте BOT_TOKEN в .env
- Убедитесь, что бот активен в @BotFather

**❌ "Video not sent"**
- Проверьте путь к файлу: `ls -la assets/`
- Используйте абсолютный путь или URL

**❌ "User hasn't started the bot"**
- Пользователь должен сначала написать боту `/start`
- Или используйте Web App для автоматической авторизации

**❌ Видео не воспроизводится**
- Формат должен быть MP4 H.264
- Попробуйте переконвертировать: `ffmpeg -i input.mp4 -c:v libx264 -c:a aac output.mp4`

---

## 📌 Итоговый чеклист:

- [ ] Видео `intro.mp4` находится в `assets/`
- [ ] Файл весит меньше 20 МБ
- [ ] В `.env` установлен `GAME_URL`
- [ ] В `.env` установлен `ENABLE_BOT_POLLING=true`
- [ ] Сервер запущен: `node server-api.js`
- [ ] Бот работает (видно сообщение "🤖 Telegram Bot запущен")
- [ ] Отправили `/start` боту в Telegram
- [ ] Получили видео с кнопкой игры
- [ ] При повторном `/start` видео не показывается

---

## 🎯 Следующие шаги:

1. **Оптимизация**: Получите file_id и используйте его вместо файла
2. **Аналитика**: Добавьте отслеживание просмотров видео
3. **A/B тест**: Попробуйте разные варианты видео
4. **Метрики**: Отслеживайте конверсию из intro в игру

---

**Готово! 🚀** Теперь ваша игра начинается с крутого видео!

💡 **Совет**: Видео должно быть коротким (5-10 сек), динамичным и сразу показывать суть игры.
